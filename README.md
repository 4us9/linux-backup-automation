# Linux Backup Automation
A job scheduller that runs a script every day to gather files that have been updated in the past 24 hours, and archiving it in a tar file. This script relies on two arguments: a target path and a destination path. Cron will automate the process and backup the files accordingly.